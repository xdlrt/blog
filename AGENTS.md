# AGENTS.md

本文件约定本仓库中所有 Agent 的工作方式。目标是保持博客的编辑感、可维护性和跨屏一致性，同时避免把临时实现、脆弱样式或无意义测试带入代码库。

## 1. 沟通与工作边界

- 始终使用简体中文沟通，先说明结果，再补充必要依据。
- 修改前先阅读相关页面、组件、布局、全局样式和测试，不要只根据截图做局部补丁。
- 保留用户已有改动。先执行 `git status --short` 和相关 diff；不得覆盖、回滚或顺手整理无关代码。
- 用户要求 Review 或诊断时，先给出基于代码与实际页面的结论；只有明确要求修复或优化时才修改代码。
- 本地预览、资源地址和 E2E 统一使用 `http://localhost:4321`，不要混用 `127.0.0.1`。
- 不把当前 PR 编号、临时进程号、一次性截图结论等写入长期规则。

## 2. 项目基线

- 技术栈：Astro 7、React 19、Tailwind CSS 4、TypeScript、Playwright。
- Node.js：使用 `package.json` 声明的 Node 24.x。
- 包管理：仓库提交 `package-lock.json`，默认使用 npm，避免在无明确理由时新增其他锁文件。
- 内容来源：
  - 博客文章：`src/data/blog/**/*.md`
  - Newsletter：`src/data/newsletter/**/*.md`
  - 关于页正文：`src/pages/about.md`
- 关键结构：
  - 页面路由：`src/pages/`
  - 页面布局：`src/layouts/`
  - 可复用组件：`src/components/`
  - 全局设计令牌与基础排版：`src/styles/base.css`
  - 内容 schema：`src/content.config.ts`、`src/schemas/`
  - 功能 E2E：`tests/`

常用命令：

```bash
npm install
npm run dev
npm run build
npm run format:check
npm run test:ui
```

## 3. 视觉方向

本博客采用克制的编辑式设计：暖纸色背景、深绿色文字与强调色、宋体标题、清晰的留白和内容层级。

- 用字号、字重、间距和颜色建立层级，少用边框、分割线、卡片套卡片和装饰性容器。
- 不引入年轮、朱砂、冗余图标、无意义箭头或仅为“增加设计感”的动效。
- 图标只有在能显著提高识别或操作效率时才使用；社交入口、Footer 链接和文章行动点优先采用明确文字。
- 动效必须服务于状态反馈，幅度克制，并遵守 `prefers-reduced-motion`。
- 延续已有色彩、字体、圆角、阴影和宽度令牌。优先扩展共享 token，不在页面中散落近似值。
- 不使用负 margin、绝对定位或逐断点像素偏移去“修正”结构问题；先修复容器、网格和对齐基线。

## 4. 页面级设计约束

### 首页首屏

- 核心信息是定位短句、主标语、文章数量与持续更新状态、主要行动入口和外部链接。
- 删除重复介绍、重复年份、装饰标签和无实际价值的说明卡片。
- 宽屏可以开放跨栏，但标题、数据、行动点和外链必须有稳定、可感知的对齐轴。
- 移动端的阅读顺序应为：主标题 → 数据状态 → 行动入口 → 外部链接；DOM 顺序应与该阅读顺序一致。

### 最近文章与归档

- 文章卡片保持等权的编辑式布局：桌面端两列、移动端单列。
- 卡片默认态与 hover 态的左边缘必须一致；hover 不应造成内容跳动或列宽变化。
- “查看完整写作档案”等主要行动点使用清晰的文字按钮或高对比文字链接，不添加箭头图标。
- 分页器提供上一页、下一页、当前页和必要的页码跳转；避免为了完整而堆叠低频控件。

### 关于页

- 正文继续由 `src/pages/about.md` 维护，不在 Markdown 中嵌入布局 HTML。
- 结构化呈现由 `src/layouts/AboutLayout.astro` 负责；内容与表现分离。
- 工作方法等网格在宽屏、笔记本、手机上分别自然降为 4、2、1 列，序号、标题和正文共享左对齐轴。
- 自定义列表或网格要显式处理 Typography 插件继承的 `padding`、marker 和间距，避免看似“差几像素”的错位。

### 文章详情

- 正文宽度应舒适且不局促，但仍以可读行长为先，复用阅读宽度 token。
- “发布于”和日期保持同一行并垂直居中，元信息应与正文有明显层级差异。
- 标签必须明显像可点击标签，同时避免过重的按钮感。
- 表格、代码块和长链接在窄屏内局部滚动或换行，不能撑破整页。

### Footer

- Footer 背景横贯视口，内部内容与主页面 shell 对齐。
- 响应式变化通过内部 grid/flex 和间距完成，不把 Footer 做成固定宽度的孤立卡片。

## 5. 响应式与对齐

- 所有视觉改动至少检查 390、768、1024、1440 和 ≥1600px；重点覆盖手机、笔记本和宽屏，而非只看一个桌面尺寸。
- 优先使用内容驱动的 Grid/Flex、`minmax()`、`clamp()` 和共享宽度变量。
- 默认阅读容器与展示容器使用 `src/styles/base.css` 中的 `--layout-reading-width` 和 `--layout-display-width`，除非页面语义确实需要例外。
- 同一内容组建立少量明确的对齐轴。标题、元信息、正文、卡片和行动点不要各自拥有随意的左边距。
- 断点改变布局关系，而不是逐个修补坐标。任何横向溢出都应定位真实来源，不通过全局 `overflow-x: hidden` 掩盖。
- 响应式验证同时检查：信息完整性、阅读顺序、可点击区域、换行、溢出、hover/focus 稳定性和深色模式。

## 6. 可访问性与交互

- 使用语义化链接、按钮、标题和列表；不要用 `div` 模拟交互控件。
- 所有交互支持键盘，focus 状态清晰可见；移动端点击目标尽量不小于 44px。
- 图标按钮必须有可访问名称；纯装饰图形应对辅助技术隐藏。
- 颜色不能是唯一状态信号，并保证浅色、深色主题均有足够对比度。
- 客户端岛屿在 hydration 前后不应出现误导性的可操作状态或明显布局跳动。

## 7. 测试边界

Playwright E2E 只验证用户可感知的功能，不作为视觉回归或像素测试工具。

应该覆盖：

- 页面可访问、关键标题和内容可见。
- 导航、文章链接、标签归档、分页和“加载更多”的目标正确。
- 搜索可输入、可产生结果并打开正确 URL。
- 移动菜单可打开、关闭并响应 Escape。
- 主题切换可工作并保持必要状态。
- 日期、标签等关键语义信息存在且可读。

不要断言：

- `getBoundingClientRect()` 的精确坐标、宽高或列数。
- 具体字号、颜色、圆角、背景、hover 色和 focus outline 像素。
- `scrollWidth`、scrollbar gutter、设计 token 或其他实现细节。
- 会随内容增长变化的固定结果数量；优先断言结果大于零或目标有意义。

其他测试原则：

- 桌面与移动端只有在交互路径确实不同的情况下分别建用例，避免重复 smoke case。
- 不使用任意 `waitForTimeout`。React/Astro 客户端组件应等待真实 hydration 或业务就绪信号。
- 视觉对齐通过浏览器在真实断点人工 Review；如项目未来引入截图基线，应建立独立视觉回归流程。

## 8. 开发与排障

- 开始视觉 Review 前确认 4321 端口运行的是当前工作区代码；必要时读取终端输出和页面实际 DOM/CSS。
- Astro scoped CSS 在连续改动或构建后偶尔可能出现 HMR 陈旧：如果 DOM 已更新但样式仍旧，先核对源码，再干净重启准确的 dev 进程并复测。
- dev server 运行期间执行 build 可能触发 Vite 依赖重新优化；若 React 岛屿不再 hydration，重启 dev server，不要用修改正确 CSS/JS 的方式迁就缓存。
- 不把偶现的本地缓存症状写成产品逻辑。

## 9. 修改与提交规范

- 文件编辑使用小而聚焦的 patch，避免无关格式化和大范围机械重写。
- 新组件优先复用现有布局、设计 token 和语义模式；只有确有多个消费者时才抽象。
- 提交前检查 `git diff --check`、相关 diff 和工作区状态。
- 使用 Conventional Commits，并保持每个提交职责单一，例如：
  - `feat: refine responsive archive layout`
  - `fix: align post metadata`
  - `test: focus e2e coverage on user flows`
  - `docs: add repository agent guidelines`
- PR 描述中的验证结果必须真实；未运行或已知失败的命令要明确说明。

## 10. 完成标准

根据改动范围执行最小但充分的验证：

1. 文档变更：`npm exec prettier -- --check AGENTS.md`、`git diff --check`。
2. 样式或组件变更：格式检查、`npm run build`、相关 E2E，并在目标断点做人工视觉 Review。
3. 路由、搜索、主题、分页等交互变更：运行相关 Playwright 用例；影响面较大时运行 `npm run test:ui`。
4. 内容 schema 或构建链路变更：至少运行 `npm run build`。

最终交付应说明：改了什么、验证了什么、是否存在已知限制，以及用户可以从哪里继续维护内容。

## 11. 已知限制

- 当前 `npm run lint` 可能因 ESLint 10 与仓库遗留配置格式不兼容而失败。除非任务包含升级 ESLint 配置，否则不要宣称 lint 已通过，也不要把该基础设施问题与业务改动混为一谈。
- 若本机 Node 版本不是 24.x，安装或构建时可能出现 engine 警告；先切换到项目要求的 Node 版本再判断代码问题。
